services:
  - type: web
    name: hoptonode
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: PLAYIT_SECRET_KEY
        sync: false
